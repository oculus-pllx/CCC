# Node npm And Playwright Ubuntu Design

Date: 2026-05-23
Status: Approved design

## Goal

Make fresh CCC installs consistently provide `npm` with Node.js, and make the
Playwright/Chromium installer honest about Ubuntu 26.04 limitations.

## Root Cause

The provisioner currently installs `nodejs` from NodeSource and immediately
prints `npm --version`, but it does not explicitly install the `npm` package.
Fresh images can therefore end up with Node available and npm missing.

Ubuntu 26.04 also keeps `chromium-browser` as a transitional package to the
Chromium snap. That is a poor fit for Proxmox LXC environments. Playwright has
also tracked Ubuntu 26.04 Chromium install support separately upstream, so CCC
should not imply that headless Chromium is equally reliable there.

## Scope

This change covers:

- Provisioner Node.js install command.
- Provisioner verification of both `node` and `npm`.
- The `ccc-install-playwright` script generated by the provisioner.
- README troubleshooting and workstation stack notes.
- Static regression checks for those installer/docs markers.

This change does not install Playwright during base provisioning, switch the
default OS away from Ubuntu 26.04, or add third-party Chromium repositories.

## Behavior

CCC provisioning should install both `nodejs` and `npm` explicitly. The output
should continue to print Node and npm versions after install.

`ccc-install-playwright` should detect Ubuntu 26.04 by reading `/etc/os-release`.
On Ubuntu 26.04, it should print a clear warning before running Playwright that:

- Ubuntu 26.04 Chromium support may lag Playwright releases.
- Ubuntu Chromium packages are snap-transitioned.
- Debian 13 is the safer CCC path when browser automation matters.

The script should still allow the user to attempt the install because Playwright
support can improve over time.

## Testing

Static checks should verify:

- The provisioner installs `nodejs npm`.
- The Playwright helper checks `VERSION_ID` and warns about Ubuntu 26.04.
- README mentions Debian 13 as the safer browser automation path.

